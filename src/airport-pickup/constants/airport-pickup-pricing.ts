/** Base GHS rate for a single passenger airport pickup. */
export const AIRPORT_PICKUP_BASE_PRICE_GHS = 150;

/** Additional GHS charge per passenger beyond the first. */
export const AIRPORT_PICKUP_PRICE_PER_EXTRA_PASSENGER_GHS = 50;

export const DEFAULT_PICKUP_LOCATION = 'Kotoka International Airport (Accra)';
